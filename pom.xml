<!--/////////////////////////////////////////////////////////////////////////////////////////////////////////////////-->
<!--                                                                                                                 -->
<!--  © 2011-2021 Telenav, Inc.                                                                                      -->
<!--  Licensed under Apache License, Version 2.0                                                                     -->
<!--                                                                                                                 -->
<!--/////////////////////////////////////////////////////////////////////////////////////////////////////////////////-->

<project xmlns:xsi = "http://www.w3.org/2001/XMLSchema-instance" xmlns = "http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation = "http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <name>Lexakai</name>

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.telenav.lexakai</groupId>
    <artifactId>lexakai</artifactId>
    <version>0.9.3</version>
    <packaging>jar</packaging>

    <!-- Metadata -->

    <description>Lexakai annotations are used by Lexakai to generate UML diagrams.</description>

    <organization>
        <name>Telenav</name>
        <url>https://www.telenav.com</url>
    </organization>

    <inceptionYear>2021</inceptionYear>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/Telenav/lexakai-annotations/issues</url>
    </issueManagement>

    <developers>
        <developer>
            <id>jonathanl</id>
            <name>Jonathan Locke (Luo Shibo)</name>
            <email>jonathanl@telenav.com</email>
            <roles>
                <role>lead</role>
                <role>administrator</role>
            </roles>
        </developer>
    </developers>

    <!-- Properties -->

    <properties>

        <lexakai.jar>lexakai-${project.version}.jar</lexakai.jar>

        <!-- KivaKit Version -->

        <kivakit.version>0.9.0-SNAPSHOT</kivakit.version>

        <!-- Java -->

        <java.version>12</java.version>

        <!-- Maven -->

        <maven-compiler-plugin.version>3.8.1</maven-compiler-plugin.version>
        <maven-compiler-plugin-java.version>12</maven-compiler-plugin-java.version>
        <maven-resources-plugin.version>2.7</maven-resources-plugin.version>
        <maven-shade-plugin.version>3.2.4</maven-shade-plugin.version>
        <maven-jar-plugin.version>2.4</maven-jar-plugin.version>
        <maven-javadoc-plugin.version>3.2.0</maven-javadoc-plugin.version>
        <maven-source-plugin.version>2.2.1</maven-source-plugin.version>
        <maven.build.timestamp.format>yyyy.MM.dd-HH.mma</maven.build.timestamp.format>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <moditect-maven-plugin.version>1.0.0.RC1</moditect-maven-plugin.version>

    </properties>

    <!-- Deployment Repositories -->

    <distributionManagement>

        <repository>
            <id>github</id>
            <name>GitHub Lexakai Apache Maven Packages</name>
            <url>https://maven.pkg.github.com/Telenav/lexakai</url>
        </repository>

    </distributionManagement>

    <!-- Repositories -->

    <repositories>

        <repository>
            <id>github-kivakit-build-public</id>
            <url>https://public:&#103;hp_YDUJnCIaQNuPJj5vb9mBsf1ylryVfO4FqFaw@maven.pkg.github.com/Telenav/kivakit-build</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>

        <repository>
            <id>github-kivakit-public</id>
            <url>https://public:&#103;hp_YDUJnCIaQNuPJj5vb9mBsf1ylryVfO4FqFaw@maven.pkg.github.com/Telenav/kivakit</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>

    </repositories>

    <!-- Dependencies -->

    <dependencies>

        <!-- KivaKit -->

        <dependency>
            <groupId>com.telenav.kivakit</groupId>
            <artifactId>kivakit-core-application</artifactId>
            <version>${kivakit.version}</version>
        </dependency>
        <dependency>
            <groupId>com.telenav.kivakit</groupId>
            <artifactId>kivakit-data-formats-xml</artifactId>
            <version>${kivakit.version}</version>
        </dependency>
        <dependency>
            <groupId>com.telenav.kivakit</groupId>
            <artifactId>kivakit-core-resource</artifactId>
            <version>${kivakit.version}</version>
        </dependency>
        <dependency>
            <groupId>com.telenav.kivakit</groupId>
            <artifactId>kivakit-core-network-http</artifactId>
            <version>${kivakit.version}</version>
        </dependency>
        <dependency>
            <groupId>com.telenav.kivakit</groupId>
            <artifactId>kivakit-core-collections</artifactId>
            <version>${kivakit.version}</version>
        </dependency>

        <!-- JavaParser -->

        <dependency>
            <groupId>com.github.javaparser</groupId>
            <artifactId>javaparser-core</artifactId>
            <version>3.19.0</version>
        </dependency>
        <dependency>
            <groupId>com.github.javaparser</groupId>
            <artifactId>javaparser-symbol-solver-core</artifactId>
            <version>3.19.0</version>
        </dependency>

    </dependencies>

    <build>

        <!-- Resource Copying -->

        <resources>
            <resource>
                <directory>src/main/java</directory>
                <includes>
                    <include>**/*</include>
                </includes>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>**/*</include>
                </includes>
            </resource>
        </resources>
        <testResources>
            <testResource>
                <directory>src/test/java</directory>
                <includes>
                    <include>**/*</include>
                </includes>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </testResource>
            <testResource>
                <directory>src/test/resources</directory>
                <includes>
                    <include>**/*</include>
                </includes>
            </testResource>
        </testResources>

        <!-- Maven Plugins -->

        <plugins>

            <!-- Java Compiler -->

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <source>${maven-compiler-plugin-java.version}</source>
                    <target>${maven-compiler-plugin-java.version}</target>
                    <release>${java.version}</release>
                    <compilerArgs>
                        <compilerArgument>-Xlint:unchecked</compilerArgument>
                        <compilerArgument>-Xlint:deprecation</compilerArgument>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- Maven Build -->

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.2.0</version>
            </plugin>

            <!-- Shaded JAR -->

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>${maven-shade-plugin.version}</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <finalName>lexakai-${project.version}</finalName>
                            <minimizeJar>false</minimizeJar>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer
                                        implementation = "org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.telenav.lexakai.Lexakai</mainClass>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>module-info.class</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <artifactSet>
                                <includes>
                                    <include>*:*</include>
                                </includes>
                            </artifactSet>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Testing -->

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.0.0-M5</version>
                <configuration>
                    <!--suppress UnresolvedMavenProperty -->
                    <excludedGroups>${exclude.test.groups}</excludedGroups>
                    <systemPropertyVariables>
                        <!--suppress UnresolvedMavenProperty -->
                        <testQuick>${testQuick}</testQuick>
                    </systemPropertyVariables>
                    <argLine>-Xmx2g</argLine>
                </configuration>
            </plugin>

            <!-- Javadoc -->

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${maven-javadoc-plugin.version}</version>
                <configuration>
                    <show>public</show>
                    <source>12</source>
                    <detectJavaApiLink>false</detectJavaApiLink>
                    <doclint>none</doclint>
                    <nohelp>true</nohelp>
                    <reportOutputDirectory>docs</reportOutputDirectory>
                    <destDir>javadoc</destDir>
                    <additionalOptions>--show-module-contents all --show-packages all --show-types protected</additionalOptions>
                </configuration>
                <executions>
                    <execution>
                        <id>aggregate</id>
                        <phase>site</phase>
                        <goals>
                            <goal>aggregate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Create project.properties and build.properties files -->

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.0.0</version>
                <executions>
                    <execution>
                        <id>project-metadata</id>
                        <phase>verify</phase>
                        <configuration>
                            <target name = "build-metadata">
                                <property name = "project.groupId" value = "${project.groupId}"/>
                                <property name = "project.artifactId" value = "${project.artifactId}"/>
                                <property name = "project.version" value = "${project.version}"/>
                                <property name = "project.name" value = "${project.name}"/>
                                <property name = "project.basedir" value = "${project.basedir}"/>
                                <condition property = "source.exists">
                                    <available file = "src" type = "dir"/>
                                </condition>
                                <taskdef resource = "net/sf/antcontrib/antcontrib.properties" classpathref = "maven.plugin.classpath"/>
                                <if>
                                    <equals arg1 = "${source.exists}" arg2 = "true"/>
                                    <then>
                                        <echo file = "src/main/java/project.properties">
                                            project-name=${project.name}${line.separator}project-version=${project.version}${line.separator}project-group-id=${project.groupId}${line.separator}project-artifact-id=${project.artifactId}${line.separator}project-basedir=${project.basedir}${line.separator}
                                        </echo>
                                        <java jar = "${maven.multiModuleProjectDirectory}/tools/building/kivakit-build-metadata-0.9.3.jar"
                                              failonerror = "true"
                                              fork = "true">
                                            <arg value = "src/main/java"/>
                                        </java>
                                    </then>
                                </if>
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>install-log-viewer-jar</id>
                        <phase>package</phase>
                        <configuration>
                            <target name = "Install ${lexakai.jar}">
                                <echo message = "Installing ${lexakai.jar}"/>
                                <copy file = "target/${lexakai.jar}"
                                      tofile = "${project.basedir}/../../../tools/applications/bin/${lexakai.jar}"/>
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>ant-contrib</groupId>
                        <artifactId>ant-contrib</artifactId>
                        <version>20020829</version>
                    </dependency>
                </dependencies>
            </plugin>

        </plugins>

    </build>

</project>
